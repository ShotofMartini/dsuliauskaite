---
import '../styles/global.css';
import Nav from '../components/Nav.astro';

interface Props {
  title?: string;
  description?: string;
}

const { title = 'Deimantė Šuliauskaitė', description = 'Fotografijos portfolio' } = Astro.props;
---

<!doctype html>
<html lang="lt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="description" content={description} />
    <title>{title}</title>
  </head>
  <body class="min-h-screen bg-white text-neutral-900">
    <Nav />
    <main>
      <slot />
    </main>
    <footer class="border-t border-neutral-200 py-8 mt-24">
      <div class="max-w-6xl mx-auto px-6 flex flex-col sm:flex-row justify-between items-center gap-4 text-sm text-neutral-500">
        <p>&copy; {new Date().getFullYear()} Deimantė Šuliauskaitė. Visos teisės saugomos.</p>
        <div class="flex gap-6">
          <a href="https://www.instagram.com/ataik_nutrauksiu/" target="_blank" rel="noopener" class="hover:text-neutral-900 transition-colors">Instagram</a>
        </div>
      </div>
    </footer>
  </body>
</html>
